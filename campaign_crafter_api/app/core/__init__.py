# This file makes the 'core' directory a Python package.

# You can optionally expose parts of your modules at the package level, for example:
# from .config import settings
# from .security import create_access_token, decode_access_token

# For now, keeping it simple. Users of this package can import directly from the modules:
# e.g., from campaign_crafter_api.app.core.config import settings
# e.g., from campaign_crafter_api.app.core.security import create_access_token
