// src/__mocks__/styleMock.js
// Mocks CSS imports for Jest.
// For CSS Modules, it would return an identity object proxy.
// For global CSS imports, it effectively makes them a no-op.
module.exports = {};
